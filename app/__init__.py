"""DanusWin ? a Windows desktop shell around the Danus proof-search engine.

Keeps the core engine intact (danus/): verifier-gated fact graph, worker swarm,
strategy loop. Adds a Chinese clickable web UI + Windows-native process
management (no bash, no WSL).
"""
__version__ = "0.1.0"
